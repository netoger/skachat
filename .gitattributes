# Рабочее дерево на Windows, а исполняется всё на Linux. Shell-скрипт
# с окончаниями CRLF там не запускается совсем: интерпретатор видит
# `\r` как часть команды. Поэтому в репозитории и при выкладке — только LF.
* text=auto eol=lf
